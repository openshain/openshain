{
  "name": "openshain-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.3"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "bun test",
    "schemas": "bun scripts/generate-schemas.ts",
    "build": "bun scripts/build.ts",
    "build:packages": "for p in core tools agent mcp cli; do (cd packages/$p && rm -rf dist && tsc -p tsconfig.build.json) || exit 1; done"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11",
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
